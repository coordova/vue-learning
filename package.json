{
  "scripts": {
    "start": "npx serve & npx json-server db.json -p 3001",
    "iniserver": "npx serve",
    "inidb": "npx json-server db.json -p 3001"
  },
  "devDependencies": {
    "json-server": "^0.17.1"
  }
}
